namespace StellarWallet.Domain.Errors
{
    public enum ErrorType
    {
        NotFound,
        Invalid,
        Conflict,
        ExternalServiceError,
        UnauthorizedError,
        InternalError
    }
}
